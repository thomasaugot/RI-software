export type chatInfoType = {
    name: string, 
    description: string,
    avatar: string | null,
    group: boolean
}

export type chatInfoParticipantCardProps = {
    avatar: string | null,
    name: string
}